"""Apply -- cover letters, the ATS answer bank, application packets.

One of JobDesk's three packages. It reads what `radar` writes and drives
`engine` through its CLI; it imports neither.

The submission line stays exactly where plan item 6 put it: this package
builds the folder and opens the link, and a human clicks submit.
"""
